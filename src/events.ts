const RECONNECTING = "reconnecting";
const RECONNECT = "reconnect";
const NEEDS_EXECUTE = "needsExecute";
const RELOAD = "reload";
const ONLINE_CHANGE = "onlineChange";
const COLLAB_ERROR = "collabError";

export {
  RECONNECTING,
  RECONNECT,
  RELOAD,
  NEEDS_EXECUTE,
  ONLINE_CHANGE,
  COLLAB_ERROR,
};
